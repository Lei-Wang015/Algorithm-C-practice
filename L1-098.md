```C
#include <stdio.h>

int main()
{
    int come = 0, go = 0;
    int ret = scanf("%d %d", &come, &go);
    if(ret != 2 || come > go){
        return 1;
    }
    printf("%d\n", go - come);
    return 0;
}
```
