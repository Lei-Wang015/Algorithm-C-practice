```C
#include <stdio.h>
int main() {
    int N = 0, v = 0;
    int ret = scanf("%d %d", &N, &v);
    if(ret != 2){
        return 1;
    }
    printf("%d\n", N / v);
    return 0;
}
```
- 基础题怎么放这么往后；
